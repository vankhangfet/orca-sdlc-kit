# Grill-Me Phase Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add an opt-in `grill` interview step (id `grill`) to the SDLC pipeline, toggled per-run by `--grill-me` / `--no-grill-me`, writing `BRAINSTORM.md` that Planning consumes.

**Architecture:** The step lives entirely in `flow.config.json` (config-driven, like every step). flow.mjs gains only flag parsing (~12 lines) that flips the `grill` step's `enabled` field after config load and before pipeline normalization, so `--only`/`--from`/read-shrinking/dry-run all see the final state with no other code changes. The interview itself happens inside the agent's Orca TUI (the user types answers into that terminal); the artifact flows to Planning via the existing `reads` mechanism.

**Tech Stack:** Node.js ESM (stdlib only), JSON configs, Markdown docs. No test framework exists in this kit (config-only workspace) — the verification contract is `node --check` plus a `--dry-run` output matrix, exactly as locked in the spec (section 7).

**Spec:** `docs/superpowers/specs/2026-08-29-grill-me-design.md`

**Environment notes for the executor:**
- Repo root for all commands: `C:\Working\FY26\DE\Multi-harness\orca\v2` (Windows, PowerShell). Git repo on branch `main`.
- `--dry-run` still calls `resolveOrca()` (flow.mjs:372) before printing, so the `orca` CLI must be on PATH (or set `ORCA_CLI_COMMAND`). It is on this dev machine.
- The dry-run cases below are chosen to be independent of whether the `name:lab2` worktree currently exists, EXCEPT case 2: it assumes no leftover `BRAINSTORM.md` in lab2's `.orca/artifacts/`. If case 2 shows `reads=[grill]` on Planning, delete `<lab2>/.orca/artifacts/BRAINSTORM.md` and re-run.

---

### Task 1: Add the `grill` step to flow.config.json

**Files:**
- Modify: `.orca/flow.config.json` (planning `reads` at line 22; new step inserted at pipeline start, line 15-16)

- [ ] **Step 1: Add `"grill"` to planning's reads** (do this FIRST — while `"reads": []` is still unique in the file)

Edit `.orca/flow.config.json`:

```json
old:
      "writes": "PLAN.md",
      "reads": [],

new:
      "writes": "PLAN.md",
      "reads": [
        "grill"
      ],
```

- [ ] **Step 2: Insert the grill step at index 0 of `pipeline`**

Edit `.orca/flow.config.json`:

```json
old:
  "pipeline": [
    {
      "id": "planning",

new:
  "pipeline": [
    {
      "id": "grill",
      "title": "Grill Me (requirements interview)",
      "enabled": false,
      "agent": "claude",
      "writes": "BRAINSTORM.md",
      "reads": [],
      "timeoutMs": 3600000,
      "hardTimeoutMs": 5400000,
      "spec": "You are running an interactive REQUIREMENTS INTERVIEW (\"grill me\") for the objective above. Your job: interrogate the user IN THIS TERMINAL until the idea is concrete enough to design and plan. Interview protocol - follow exactly: (1) Ask ONE question at a time and WAIT for the user's answer in this terminal before asking the next. Never batch questions. (2) Prefer multiple choice (a/b/c/...) over open-ended; open-ended only when options would bias the answer. (3) Cover, in roughly this order: purpose and success criteria; scope (explicitly in/out); users and UX expectations; technical and business constraints; edge cases; risks; open decisions that planning cannot make alone. (4) After each answer, restate your understanding in one short line so the user can correct you early. (5) When you have enough information, present a numbered DECISIONS list (each: decision + rationale) and ask the user to confirm or edit each one; do not proceed until every line is confirmed. (6) This phase is REQUIREMENTS ONLY - do not write code, tests, or detailed technical design. When all decisions are confirmed, write the result to {out} with exactly these sections:\n# Brainstorm\n## Objective (refined) - 1-3 sentences agreed with the user\n## Decisions - numbered; each with rationale and marked \"confirmed by user\"\n## Scope - two lists: In / Out\n## Constraints & assumptions\n## Open questions - only if any remain; each with why it blocks nothing\nThen finish and report success."
    },
    {
      "id": "planning",
```

(`timeoutMs` 60 min / `hardTimeoutMs` 90 min are mandatory per spec section 5.1: the TUI is idle while waiting for a human, and the 15-min default would report it hung mid-interview.)

- [ ] **Step 3: Verify the config parses and the default plan is unchanged**

Run: `node .orca/flow.mjs --dry-run "x"`

Expected: 9 steps run (Planning first … Documentation last); Planning shows `reads=[-]`; a line `Skipped: grill` at the end. (Planning's reads shrink to empty because grill is disabled and no `BRAINSTORM.md` exists yet — that is the effectiveReads rule, flow.mjs:97-104.)

- [ ] **Step 4: Commit**

```bash
git add .orca/flow.config.json
git commit -m "feat: add opt-in grill interview step to SDLC pipeline config"
```

---

### Task 2: `--grill-me` / `--no-grill-me` flags in flow.mjs

**Files:**
- Modify: `.orca/flow.mjs` (usage comment lines 24-25; opt init line 45; parse loop lines 51-52; config-load block lines 63-66)

- [ ] **Step 1: Extend the usage comment**

Edit `.orca/flow.mjs`:

```js
old:
// Run a DIFFERENT pipeline config (e.g. the bug-fix flow in fixbug.config.json):
//   node .orca/flow.mjs --config fixbug.config.json "<bug description>"

new:
// Run a DIFFERENT pipeline config (e.g. the bug-fix flow in fixbug.config.json):
//   node .orca/flow.mjs --config fixbug.config.json "<bug description>"
//
// Opt-in requirements interview before planning (step id "grill" in config):
//   node .orca/flow.mjs --grill-me "..."
// Disable it for this run when it is enabled in config:
//   node .orca/flow.mjs --no-grill-me "..."
```

- [ ] **Step 2: Parse the flags**

Edit `.orca/flow.mjs`:

```js
old:
const opt = { from: null, only: null, dryRun: false, config: null, agentOverrides: {} };

new:
const opt = { from: null, only: null, dryRun: false, config: null, agentOverrides: {}, grillMe: undefined };
```

```js
old:
  else if (a === "--dry-run") opt.dryRun = true;

new:
  else if (a === "--dry-run") opt.dryRun = true;
  else if (a === "--grill-me" || a === "--no-grill-me") {
    if (opt.grillMe !== undefined) die("--grill-me and --no-grill-me are mutually exclusive.");
    opt.grillMe = a === "--grill-me";
  }
```

- [ ] **Step 3: Apply the override after config load, before normalization**

Edit `.orca/flow.mjs`:

```js
old:
catch (e) { die(`Could not read ${CONFIG_FILE}: ${e.message}`); }

const ART_DIR = cfg.artifactsDir || ".orca/artifacts";

new:
catch (e) { die(`Could not read ${CONFIG_FILE}: ${e.message}`); }

// --grill-me / --no-grill-me: one-shot override of the interview step's enabled
// state (id convention: "grill"). Applied BEFORE normalization so --only/--from
// and read-shrinking all see the final state.
if (opt.grillMe !== undefined) {
  const grill = (cfg.pipeline || []).find((s) => s && s.id === "grill");
  if (!grill) die('--grill-me: this config has no step with id "grill".');
  grill.enabled = opt.grillMe;
}

const ART_DIR = cfg.artifactsDir || ".orca/artifacts";
```

- [ ] **Step 4: Syntax check**

Run: `node --check .orca/flow.mjs`
Expected: no output, exit 0.

- [ ] **Step 5: Verify enabling**

Run: `node .orca/flow.mjs --dry-run --grill-me "x"`

Expected: 10 steps; step 1 is `Grill Me (requirements interview)` with `reads=[-]` and `writes=BRAINSTORM.md`; Planning shows `reads=[grill]`; NO `Skipped:` line.

- [ ] **Step 6: Verify mutual exclusion fails loudly**

Run: `node .orca/flow.mjs --dry-run --grill-me --no-grill-me "x"`

Expected: `[orca-flow] ERROR: --grill-me and --no-grill-me are mutually exclusive.`, exit code 1.

- [ ] **Step 7: Verify standalone brainstorm subset**

Run: `node .orca/flow.mjs --dry-run --grill-me --only grill "x"`

Expected: exactly one step runs (`Grill Me (requirements interview)`); `Skipped:` lists the other 9 ids.

- [ ] **Step 8: Commit**

```bash
git add .orca/flow.mjs
git commit -m "feat: add --grill-me/--no-grill-me flags toggling the grill step"
```

---

### Task 3: README.md — grill section, flags, examples

**Files:**
- Modify: `.orca/README.md` (flags table after line 68; examples block lines 70-75; new section before `## Configure` line 77)

- [ ] **Step 1: Add the two flag rows**

Edit `.orca/README.md`:

```markdown
old:
| `--agent <id>=<agent>` | Override one step's agent for this run. E.g. `--agent coding=claude`. |

new:
| `--agent <id>=<agent>` | Override one step's agent for this run. E.g. `--agent coding=claude`. |
| `--grill-me` | Run the optional `grill` interview step before Planning (see Grill-me below). |
| `--no-grill-me` | Skip the `grill` step for this run, even if enabled in config. |
```

- [ ] **Step 2: Add grill examples to the Examples block**

Edit `.orca/README.md`:

```markdown
old:
node .orca/flow.mjs --only planning,architecture,detailed-design "Design phase only"
```

new:
node .orca/flow.mjs --only planning,architecture,detailed-design "Design phase only"
node .orca/flow.mjs --grill-me "Interview me first, then build"
node .orca/flow.mjs --grill-me --only grill "Just the brainstorm session"
```

(Note: keep the surrounding triple-backtick fence of the Examples block intact — the old/new above show only its inner lines.)

- [ ] **Step 3: Insert the Grill-me section before `## Configure — edit ...`**

Edit `.orca/README.md`:

```markdown
old:
## Configure — edit `.orca/flow.config.json`

new:
## Grill-me — optional interview phase (opt-in)

The kit ships a `grill` step (id `grill`) with `"enabled": false`. It is an
interactive REQUIREMENTS INTERVIEW: the agent asks you questions **in its own
terminal** — one at a time, mostly multiple choice — restates each answer, then
presents every decision for you to confirm or edit. You participate by typing
into that terminal. When confirmed, it writes `BRAINSTORM.md`, which Planning
reads as input.

Turn it on for one run:

```
node .orca/flow.mjs --grill-me "Feature idea"
```

Brainstorm-only session (interview now, build later):

```
node .orca/flow.mjs --grill-me --only grill "Feature idea"
```

`BRAINSTORM.md` persists in `.orca/artifacts/`, so a later full run picks it up
automatically — Planning's `reads` already include `grill`, and a skipped step's
artifact still counts when the file exists.

Make it default-on by setting `"enabled": true` on the `grill` step in
`flow.config.json`; `--no-grill-me` then disables it for a single run. The step
carries large timeouts (60 min silence / 90 min hard) because the terminal sits
idle while it waits for a human answer — do not lower them to the 15-min default.

## Configure — edit `.orca/flow.config.json`
```

(The inner triple-backtick blocks are real content of the README — make sure the final file's fences nest correctly around the two command examples.)

- [ ] **Step 4: Commit**

```bash
git add .orca/README.md
git commit -m "docs: document the grill-me interview phase and flags"
```

---

### Task 4: CONFIGURATION.md — flags, id convention, timeouts

**Files:**
- Modify: `.orca/CONFIGURATION.md` (flags table lines 195-201; precedence paragraph lines 203-204; new subsection after line 220)

- [ ] **Step 1: Add the two flag rows**

Edit `.orca/CONFIGURATION.md`:

```markdown
old:
| `--agent <id>=<agent>` | Override one step's agent, this run only | `--agent coding=claude` |

new:
| `--agent <id>=<agent>` | Override one step's agent, this run only | `--agent coding=claude` |
| `--grill-me` | Enable the `grill` step for this run (see 5.2) | `--grill-me` |
| `--no-grill-me` | Disable the `grill` step for this run (see 5.2) | `--no-grill-me` |
```

- [ ] **Step 2: Caveat the precedence sentence**

Edit `.orca/CONFIGURATION.md`:

```markdown
old:
Flags affect only that run; they are not written to the config. `enabled:false` in
config is always respected, even if that step is listed in `--only`.

new:
Flags affect only that run; they are not written to the config. `enabled:false` in
config is always respected, even if that step is listed in `--only`. (Exception:
`--grill-me` / `--no-grill-me` explicitly override the `grill` step — see 5.2.)
```

- [ ] **Step 3: Add subsection 5.2 after the "Multiple pipelines" block**

Edit `.orca/CONFIGURATION.md` (insert after the line `Pass the complete bug context as the objective — the RCA step reproduces from it.` and its following blank line, before `---`):

```markdown
### 5.2. The `grill` step and `--grill-me` / `--no-grill-me`

`--grill-me` enables the step whose `id` is exactly `grill` (the shipped
requirements-interview step) for this run; `--no-grill-me` disables it. Both are
shorthands for flipping that step's `enabled` field. Because the override is
applied before `--only` / `--from` are processed:

- `--grill-me --only grill,planning` works (flag first, subset after).
- `--from planning` still skips the interview — a resume must not restart it.
- If the config has no step with id `grill`, the flag fails with a clear error.
- The two flags are mutually exclusive.
- Renaming the step's `id` breaks the flags — keep the id `grill`.

The shipped `grill` step carries large `timeoutMs` (60 min) / `hardTimeoutMs`
(90 min): the agent terminal is idle while it waits for a HUMAN to answer, and
the 15-min default would report the step hung mid-interview. Tune them only
upward for longer interviews.
```

- [ ] **Step 4: Commit**

```bash
git add .orca/CONFIGURATION.md
git commit -m "docs: reference grill flags, id convention, and timeout rationale"
```

---

### Task 5: Full verification matrix (spec section 7)

**Files:**
- Create (temporary, deleted in Step 4): `.orca/grill-on.config.json`

- [ ] **Step 1: Syntax + default-off**

Run: `node --check .orca/flow.mjs && node .orca/flow.mjs --dry-run "x"`

Expected: syntax check silent; plan shows 9 steps, `Skipped: grill`, Planning `reads=[-]`.

- [ ] **Step 2: Enable + standalone + both-flags + --from interplay**

Run each and check:

```
node .orca/flow.mjs --dry-run --grill-me "x"
```
Expected: 10 steps, `Grill Me (requirements interview)` first, Planning `reads=[grill]`, no `Skipped:` line.

```
node .orca/flow.mjs --dry-run --grill-me --only grill "x"
```
Expected: only `Grill Me (requirements interview)` runs; the other 9 ids in `Skipped:`.

```
node .orca/flow.mjs --dry-run --grill-me --no-grill-me "x"
```
Expected: ERROR mutually-exclusive message, exit 1.

```
node .orca/flow.mjs --dry-run --grill-me --from planning "x"
```
Expected: steps start at Planning; `Skipped: grill` (the interview must not restart on resume).

```
node .orca/flow.mjs --dry-run --grill-me --config fixbug.config.json "x"
```
Expected: `[orca-flow] ERROR: --grill-me: this config has no step with id "grill".`, exit code 1 (fixbug.config.json has no grill step — verifies the missing-step die path without a temp file).

- [ ] **Step 3: `--no-grill-me` against an enabled grill (temp config)**

```powershell
Copy-Item .orca\flow.config.json .orca\grill-on.config.json
```

Then Edit `.orca/grill-on.config.json`: replace the grill step's `"enabled": false,` with `"enabled": true,` (the only occurrence of `"enabled": false` in this file).

```
node .orca/flow.mjs --dry-run --no-grill-me --config grill-on.config.json "x"
```

Expected: 9 steps, `Skipped: grill`, Planning `reads=[-]`. Also run the same command WITHOUT `--no-grill-me` once — expected 10 steps (sanity: the temp config itself is valid).

- [ ] **Step 4: Clean up**

```powershell
Remove-Item .orca\grill-on.config.json
```

- [ ] **Step 5: Final state check + commit (only if fixes were needed)**

Run: `git status --short`
Expected: clean tree (nothing new to commit if all cases passed on first try; commit fixes with `fix: ...` if any case required a code/doc correction).

---

## Spec coverage map (self-review)

| Spec section | Covered by |
|---|---|
| 5.1 grill step + planning reads + timeouts | Task 1 |
| 5.2 interview spec full text | Task 1 Step 2 |
| 5.3 flow.mjs flags (~12 lines) + precedence | Task 2 |
| 5.4 README (table/flags/recipe) | Task 3 |
| 5.5 CONFIGURATION (flags/convention/timeouts) | Task 4 |
| 6 edge cases (mutual exclusion, missing step, --only, stale artifact note) | Task 2 Steps 5-7, Task 5 |
| 7 verification matrix (7 cases) | Task 5 |

No placeholders; all code/doc content is final text. Names consistent: step id `grill`, artifact `BRAINSTORM.md`, flags `--grill-me`/`--no-grill-me`, `opt.grillMe` (undefined = not given).
