{
  "//": "cr.config.json — CHANGE-REQUEST (CR) MAINTENANCE PIPELINE. Run with:",
  "//usage": "node .orca/flow.mjs --config cr.config.json \"<the change request: what changes and why, on the existing system>\"",
  "//steps": "impact-analysis -> cr-plan -> cr-coding -> code-review -> testing -> cr-verify. Review/Test/Verify FAIL loops back to cr-coding (maxRetries).",
  "artifactsDir": ".orca/artifacts",
  "maxRetries": 2,
  "//autoRun": "true (default) = fully automatic, no user confirmation; claude agents start with permission bypass. See flow.config.json for details.",
  "autoRun": true,
  "//worktree": "null (recommended) = auto-detect from the directory you run the flow in. Pin only when launching from OUTSIDE the target worktree. See flow.config.json for details.",
  "//model": "Optional \"model\" per step, or \"defaults.model\" for the whole pipeline. \"default\"/missing = the agent's own default model. See flow.config.json for details.",
  "//readiness": "Readiness gate (v2): declared 'reads' artifacts are verified (present, >= readinessMinBytes) before a step launches; unready inputs prompt [y/N] and can re-run the producers (readinessRetries). See flow.config.json / CONFIGURATION.md for details.",
  "defaults": {
    "timeoutMs": 900000,
    "worktree": null,
    "model": "default",
    "readinessRetries": 3,
    "readinessMinBytes": 200
  },
  "pipeline": [
    {
      "id": "impact-analysis",
      "title": "Impact Analysis",
      "enabled": true,
      "agent": "claude",
      "writes": "IMPACT_ANALYSIS.md",
      "reads": [],
      "spec": "You are a maintenance analyst. The OVERALL OBJECTIVE is a change request (CR) on an EXISTING system. Explore the current codebase and determine the impact of the requested change: the modules, files and functions affected (and why each is touched), current behavior vs desired behavior at each point, the data/config/API surface that changes, and the regression surface - the existing behaviors and tests most likely to break. If design artifacts from earlier runs exist in the artifacts dir (ARCHITECTURE.md, DETAILED_DESIGN.md, and similar), use them as context. Write {out} with sections: Change summary (restated from the objective), Affected areas (file by file), Current vs desired behavior, Regression surface, Risks and unknowns. Do NOT design the solution and do NOT write code - that is the next step's job."
    },
    {
      "id": "cr-plan",
      "title": "CR Implementation Plan",
      "enabled": true,
      "agent": "claude",
      "writes": "CR_PLAN.md",
      "reads": [
        "impact-analysis"
      ],
      "spec": "You are a planning agent. Based on {reads}, write {out}: the chosen approach (with alternatives considered and why), the exact files/functions to change and the change for each, compatibility and migration concerns for existing consumers of anything touched (APIs, configs, stored data), a test plan - new tests for the new behavior plus the existing tests that cover the regression surface - and an ACCEPTANCE CRITERIA checklist: one checkbox per point of the original CR, each concrete enough for a verifier to check mechanically. Keep the change minimal - no refactors beyond the CR."
    },
    {
      "id": "cr-coding",
      "title": "CR Coding",
      "enabled": true,
      "agent": "codex",
      "writes": "CR_CHANGES.md",
      "progress": "CR_TASKS.md",
      "reads": [
        "impact-analysis",
        "cr-plan"
      ],
      "onFailGoto": null,
      "hardTimeoutMs": 7200000,
      "spec": "You are a coding agent. FIRST ACTION - TASK CHECKLIST: before changing any code, break the change down (from {reads}) into concrete tasks and write them as a Markdown checklist to {tasks} using exactly these markers (if {tasks} already exists from an earlier attempt, read it first and continue it instead of rewriting): '- [ ]' not started, '- [~]' in progress, '- [x]' completed; one task per line, sub-tasks indented two spaces, nothing else on those lines. This file is read live by the pipeline's status page, so keep it current: mark a task '- [~]' the moment you start it and '- [x]' the moment you finish it - never batch updates at the end. Every task must be '- [x]' before you write {out}. Then implement the change request exactly per {reads}: the minimal code change AND the tests from the plan's test plan. Run the new tests and the existing tests that cover the regression surface before finishing. If this task is being re-run after a failed review, test or verification, also read {tasks}, CR_REVIEW.md, CR_TEST_REPORT.md and ACCEPTANCE.md in the artifacts dir and address every issue reported there (update the checklist, not just the reports). When done, write {out}: files changed, what changed and why, and test results."
    },
    {
      "id": "code-review",
      "title": "Code Review",
      "enabled": true,
      "agent": "claude",
      "timeoutMs": 1800000,
      "hardTimeoutMs": 7200000,
      "writes": "CR_REVIEW.md",
      "reads": [
        "cr-coding",
        "cr-plan"
      ],
      "onFailGoto": "cr-coding",
      "spec": "You are a reviewer. Compare the changed code (see {reads}) against the plan. Write {out}: findings by severity (blocker/major/minor), correctness issues, missed acceptance criteria, side effects on the regression surface, maintainability. Give a clear VERDICT at the end: if there is any blocker/major, report FAIL (outcome=failed) to loop back to coding; otherwise PASS."
    },
    {
      "id": "testing",
      "title": "Testing (incl. regression)",
      "enabled": true,
      "agent": "opencode",
      "writes": "CR_TEST_REPORT.md",
      "reads": [
        "cr-coding",
        "code-review"
      ],
      "onFailGoto": "cr-coding",
      "spec": "You are a testing agent. Based on {reads}: (1) write and RUN tests for the new or changed behavior per {reads}; (2) run the project's existing test suite as a regression check - a change request must not break existing behavior. Write {out}: tests written, commands run, pass/fail results, failure details, and fix suggestions. If any test fails, report outcome=failed to loop back to coding."
    },
    {
      "id": "cr-verify",
      "title": "Acceptance Verification",
      "enabled": true,
      "agent": "opencode",
      "writes": "ACCEPTANCE.md",
      "reads": [
        "cr-coding",
        "cr-plan",
        "impact-analysis"
      ],
      "onFailGoto": "cr-coding",
      "spec": "You are a verification agent. Based on {reads}: (1) go through the ACCEPTANCE CRITERIA checklist in the plan one by one and check each against the actual code - run the app/tests/build where possible, do not just read; (2) spot-check the regression surface from the impact analysis. Write {out}: for every criterion, what you ran or inspected and the actual result (include command output); then a clear VERDICT at the end - if any criterion is unmet or a regression is found, report FAIL (outcome=failed) to loop back to the fix; otherwise PASS."
    }
  ]
}
