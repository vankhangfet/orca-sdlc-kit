{
  "defaults": {
    "timeoutMs": 2000, "hardTimeoutMs": 25000,
    "startRetryDelayMs": 200, "warmupTimeoutMs": 5000, "startGraceMs": 3000,
    "nudgeRetries": 2, "nudgeTimeoutMs": 3000
  },
  "pipeline": [
    { "id": "alpha", "title": "Alpha", "agent": "stub", "spec": "Write {out}.", "writes": "A.md" }
  ]
}
